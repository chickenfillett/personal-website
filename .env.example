# Selects the standalone Next.js runtime. Static deployments use npm run build:static.
DEPLOY_TARGET=server
HOSTNAME=0.0.0.0
PORT=3000

# Reserved for future server features. Keep real values in deployment secrets.
# DATABASE_URL=
